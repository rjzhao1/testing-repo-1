on:
  push:
    branches:
      - add-multiple-changelogPath
name: release-please
jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      # - uses: google-github-actions/release-please-action@v3
      #   with:
      #     release-type: node
      #     package-name: testing-repo
      #     command: manifest
    - name: copy file
      uses: canastro/copy-file-action@master
      with:
        source: "CHANGELOG.md"
        target: "public/CHANGELOG.md"
